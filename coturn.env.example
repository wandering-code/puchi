# Debe existir también como coturn.env (gitignored) con los valores reales.
# COTURN_STATIC_SECRET debe coincidir EXACTAMENTE con el mismo valor en
# backend/.env — es lo que permite al backend generar credenciales que
# coturn acepte.
COTURN_STATIC_SECRET=
COTURN_EXTERNAL_IP=
COTURN_INTERNAL_IP=
